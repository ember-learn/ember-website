diff --git a/dist/services/page-title.js b/dist/services/page-title.js
index 398cb6498246cdd2ef37932e16cbb9c0da99ab3a..f23473e686d71b104a0572d0c22fe1f52f49af43 100644
--- a/dist/services/page-title.js
+++ b/dist/services/page-title.js
@@ -4,7 +4,7 @@ import Service, { service } from '@ember/service';
 import { isEmpty } from '@ember/utils';
 import { assert } from '@ember/debug';
 
-var _dec, _dec2, _class, _descriptor, _descriptor2;
+var _dec, _dec2, _dec3, _class, _descriptor, _descriptor2, _descriptor3;
 const isFastBoot = typeof FastBoot !== 'undefined';
 const RouterEvent = {
   ROUTE_DID_CHANGE: 'routeDidChange'
@@ -26,13 +26,14 @@ const configKeys = ['separator', 'prepend', 'replace'];
   @class page-title
   @extends Ember.Service
  */
-let PageTitleService = (_dec = service('router'), _dec2 = service('-document'), (_class = class PageTitleService extends Service {
+let PageTitleService = (_dec = service('router'), _dec2 = service('-document'), _dec3 = service('head-data'), (_class = class PageTitleService extends Service {
   constructor(owner) {
     super(owner);
     _initializerDefineProperty(this, "router", _descriptor, this);
     // in fastboot context "document" is instance of
     // ember-fastboot/simple-dom document
     _initializerDefineProperty(this, "document", _descriptor2, this);
+    _initializerDefineProperty(this, "headData", _descriptor3, this);
     _defineProperty(this, "tokens", []);
     _defineProperty(this, "_defaultConfig", {
       // The default separator to use between tokens.
@@ -263,6 +264,11 @@ let PageTitleService = (_dec = service('router'), _dec2 = service('-document'),
   // eslint-disable-next-line @typescript-eslint/no-unused-vars
   titleDidUpdate(_title) {
     // default is empty, meant to be overriden by user if desired
+    if (!this.headData) {
+      return;
+    }
+
+    this.headData.pageTitle = _title;
   }
 }, (_descriptor = _applyDecoratedDescriptor(_class.prototype, "router", [_dec], {
   configurable: true,
@@ -274,6 +280,11 @@ let PageTitleService = (_dec = service('router'), _dec2 = service('-document'),
   enumerable: true,
   writable: true,
   initializer: null
+}), _descriptor3 = _applyDecoratedDescriptor(_class.prototype, "headData", [_dec3], {
+  configurable: true,
+  enumerable: true,
+  writable: true,
+  initializer: null
 })), _class));
 
 export { PageTitleService as default };
