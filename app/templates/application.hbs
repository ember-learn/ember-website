{{#es-header}}
  {{#es-navbar links=links}}
    <form>
      {{search-input projectVersion=page.currentVersion}}
    </form>
  {{/es-navbar}}
{{/es-header}}

<main class="container">
  {{outlet}}
</main>

{{es-footer}}
